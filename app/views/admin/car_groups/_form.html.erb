<%= form_for [:admin, @car_group], local: true do |form| %>

  <% if @car_group.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@car_group.errors.count, "error") %> prohibited
        this car group from being saved:
      </h2>
      <ul>
        <% @car_group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>